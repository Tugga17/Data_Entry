transform_frame = tk.Frame(self.root)
transform_frame.pack(pady=5)

self.select_transform_button = tk.Button(transform_frame, text="Select Files to Transform", command=self.select_transform_files)
self.select_transform_button.pack(side="left")

tk.Button(transform_frame, text="?", command=lambda: messagebox.showinfo("Help", "Select destination Excel files to prepare for Perfman upload.\nThey’ll be converted and combined into a single master file."), width=2).pack(side="left", padx=4)